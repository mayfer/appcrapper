
export interface FileInfo {
  // path: string;
  relativePath: string;
  name: string;
  isDirectory: boolean;
}

export interface FileChunk {
  // path: string;
  relativePath: string;
  chunk: string;
}
